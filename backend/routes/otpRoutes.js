const express = require("express");
const router = express.Router();
//const { getOtp, verifyOtp } = require("../controller/getOtp");
// router.route("/:phone/:code").get(getOtp);
// router.route("/:phone/:code/:otp").get(verifyOtp);
module.exports = router;
